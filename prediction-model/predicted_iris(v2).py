# -*- coding: utf-8 -*-
"""predicted_iris(v. 2).ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1Fb4EJ7RwdqfVWmuBs5d2ep8bNhu7-LWo

***Предсказание принадлежности к виду (датасет Iris)***

**Загрузка библиотек**
"""

import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.model_selection import RandomizedSearchCV
from sklearn.tree import DecisionTreeClassifier
from sklearn.datasets import load_iris

"""**Загрузка датасета**"""

iris = load_iris()
X = iris.data
y = iris.target

"""**Разделение данных на тестовые и тренировочные**"""

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.25)

"""**Обучение модели и поиск наилучших параметров для модели**"""

dt = DecisionTreeClassifier()
parametrs = {'criterion': ['entropy'],
             'max_depth': range(1,10),
             'min_samples_split': range(2,10),
             'min_samples_leaf': range(1,10)}
search = RandomizedSearchCV(dt, parametrs)
search.fit(X_train, y_train)

best_tree = search.best_estimator_
best_tree

"""**Предсказание**

Предсказанные значения
"""

predicted = best_tree.predict(X_test)
predicted

"""Тестовые значения"""

y_test